import Pronostico from "@/components/reporte/sections/pronostico/pronostico";

export default function Reporte() {
  return (
    <section className="flex flex-col gap-8">
      <Pronostico />
    </section>
  );
}
